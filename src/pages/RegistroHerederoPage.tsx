import React from 'react';
import { RegistroHeredero } from '@/features/herederos/components/RegistroHeredero';

const RegistroHerederoPage: React.FC = () => {
  return <RegistroHeredero />;
};

export default RegistroHerederoPage;
